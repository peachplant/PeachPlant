<h2>Listings</h2>

<%= link_to 'New Listing', new_listing_path %>

<div id="search-results">
  <ul>
  <% @listings.each do |listing| %>
    <%= render listing %>
  <% end %>
  </ul>
</div>

